apiVersion: v1
kind: Service
metadata:
  name: {{ include "wemixfi-backend.fullname" . }}
  labels:
    {{- include "wemixfi-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wemixfi-backend.selectorLabels" . | nindent 4 }}
