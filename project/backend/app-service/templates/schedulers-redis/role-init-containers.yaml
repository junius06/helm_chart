apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "wemixfi-backend.serviceAccountName" . }}-role
  labels:
    {{- include "wemixfi-backend.labels" . | nindent 4 }}
rules:
- apiGroups: ["", "batch", "apps"]
  resources:
  - pods
  - jobs
  - statefulsets
  verbs:
  - get
  - watch
  - list