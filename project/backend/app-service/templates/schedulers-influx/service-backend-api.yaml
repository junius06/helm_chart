apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-backend.fullname" . }}
  labels:
    {{- include "project-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "project-backend.selectorLabels" . | nindent 4 }}
