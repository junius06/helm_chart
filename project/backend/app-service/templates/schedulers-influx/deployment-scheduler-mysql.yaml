{{ range .Values.scheduler.mysql.list }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "project-backend.fullname" $ }}-mysql-scheduler-{{ . | replace "_" "-" | replace "." "-" }}
  labels:
    {{- include "project-backend.labels" $ | nindent 4 }}
spec:
  {{- if not $.Values.autoscaling.enabled }}
  replicas: {{ $.Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "project-backend.selectorLabels" $ | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "project-backend.labels" $ | nindent 8 }}
    spec:
      {{- with $.Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml $ | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "project-backend.serviceAccountName" $ }}
      initContainers:
      - name: {{ $.Release.Name }}-{{ . | replace "_" "-" | replace "." "-" }}-mysql-check
        image: "bitnami/kubectl:1.26.10"
        command: ["sh", "-c", "kubectl wait --timeout=300s --for=condition=complete job/{{ include "project-backend.fullname" $ }}-initializer-mysql"]
      containers:
        - name: {{ $.Chart.Name }}
          image: "{{ $.Values.scheduler.mysql.image }}:{{ $.Values.scheduler.tag }}"
          imagePullPolicy: {{ $.Values.scheduler.pullPolicy }}
          resources:
            {{- toYaml $.Values.resources | nindent 12 }}
          env:
            - name: TARGET_SCHEDULE
              value: {{ . }}
          envFrom:
            - configMapRef:
                name: {{ include "project-backend.fullname" $ }}-common-configmap

{{ end }}